# Nextcloud WebDAV configuration for `npm run fetch`
NEXTCLOUD_BLOG_WEBDAV_URL=https://nextcloud.example.com/remote.php/dav/files/<username>/Notes/
NEXTCLOUD_USERNAME=<username>
NEXTCLOUD_APP_PASSWORD=<app-password>

# Optional: override directories
# NEXTCLOUD_POSTS_SUBDIR=posts/ # set empty to use blog root
# LOCAL_POSTS_DIR=content

# Site configuration (VITE_ prefix makes them available in browser)
VITE_SITE_NAME=Max Zhang
VITE_COPYRIGHT_YEAR=2024

# Beian (备案) configuration
VITE_ICP_NUMBER=ICP备XXXXXXXXXXXXXX号-1
VITE_ICP_URL=https://beian.miit.gov.cn/
VITE_BEIAN_NUMBER=网安备XXXXXXXXXXXXXXXX号
VITE_BEIAN_URL=http://www.beian.gov.cn/portal/registerSystemInfo

# Deployment configuration for `npm run deploy`
DEPLOY_HOST=your-server.com              # 服务器地址或 IP
DEPLOY_USER=www-data                     # SSH 用户名
DEPLOY_PATH=/var/www/html/blog           # 服务器上的目标路径
DEPLOY_PORT=22                           # SSH 端口（默认 22）

# 认证方式（三选一）：
# 1. 使用默认 SSH 密钥（推荐，~/.ssh/id_ed25519 或 ~/.ssh/id_rsa）
# 2. 指定 SSH 密钥路径：
# DEPLOY_SSH_KEY_PATH=/path/to/ssh/key
# 3. 使用密码（不推荐，明文存储）：
# DEPLOY_PASSWORD=your_password
