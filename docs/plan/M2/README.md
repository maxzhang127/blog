# M2：完善（标签/分类/搜索 + 体验与质量）

## 目标

- 交付标签/分类浏览与元数据搜索，形成完整的信息架构闭环。
- 完成移动端适配与可访问性验收，确保跨浏览器可用。
- 固化构建性能与稳定性基线，并完善文档与发布流程。

## 前置依赖

- M1 核心页面与索引生成已完成并稳定。
- `assets/` 策略已落地且资源 URL 在运行时可访问。

## 工作包清单

- `M2-T1`：标签/分类聚合页（见 `docs/plan/README.md`）
- `M2-T2`：元数据搜索（见 `docs/plan/README.md`）
- `M2-T3`：移动端适配与可访问性验收（见 `docs/plan/README.md`）
- `M2-T4`：构建性能与稳定性验收（见 `docs/plan/README.md`）
- `M2-T5`：文档与发布流程固化（见 `docs/plan/README.md`）

详细拆分与验收口径：`docs/plan/M2/work-packages.md`

## 里程碑退出标准（验收）

- 标签/分类/搜索功能可用并具备清晰空态与返回入口。
- 移动端阅读体验达标；键盘导航与语义化标题等可访问性要求通过验收。
- 构建可复现、稳定；关键指标（耗时/体积）有基线记录；文档可指导新环境复现构建与发布。
