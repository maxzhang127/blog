# Public Assets

此目录用于存放静态资源文件。

## 需要的图标文件

Footer组件需要以下备案图标（可选）：

- `icp.png` - ICP备案图标（16x16px）
- `beian.png` - 公安备案图标（16x16px）

如果这些图标不存在，Footer仍然可以正常显示，只是不会显示图标。

## 如何添加图标

1. 将图标文件放在此目录下
2. 确保文件名为 `icp.png` 和 `beian.png`
3. 重新构建项目

## 当前文件

- `content/` - 构建时生成的内容文件（posts、index等）
